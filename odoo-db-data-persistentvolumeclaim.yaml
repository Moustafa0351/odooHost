apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: odoo-db-data
  name: odoo-db-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
